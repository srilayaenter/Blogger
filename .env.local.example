# This project currently needs no environment variables to build or run.
#
# The site is a static export (see docs/architecture.md and next.config.ts) that reads content
# from content/*.json at build time -- there is no database, no API endpoint, and no secret to
# configure. This file is kept as a placeholder in case a future stage needs one.
